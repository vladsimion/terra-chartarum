---
title: Anatomy of a Native Essay
subtitle: A Worked Example of the Component Library
summary: >-
  A native (MDX) essay that doubles as living documentation: it uses every shared
  island — radar, adaptive timeline, compare slider, and scrollytelling — so new
  authors can copy it as a starting point.
cover: /covers/starter-example.svg
status: native
eras: [Renaissance, Enlightenment, Modern]
regions: [Global]
lenses: [Measure, Witness, Use, Cosmos, Power, Silence]
yearFrom: 1500
yearTo: 2024
mapCount: 3
readingMinutes: 4
accent: '#96cc84'
order: 99
featured: false
room: map
sections:
  - id: on-abstraction
    title: Why these components are theatre
    room: theatre
    secondaryRooms: [map]
publishedAt: '2026-07-15'
updatedAt: '2026-07-15'
metaScores:
  measure: 0.8
  witness: 0.7
  use: 0.6
  cosmos: 0.4
  power: 0.55
  silence: 0.45
---

import RadarChart from '../../components/islands/RadarChart.astro';
import AdaptiveTimeline from '../../components/islands/AdaptiveTimeline.astro';
import CompareSlider from '../../components/islands/CompareSlider.astro';
import Scrollytelling from '../../components/islands/Scrollytelling.astro';
import Section from '../../components/Section.astro';

This page is authored in **MDX** and rendered natively inside the portal shell —
no iframe. It exists to show the three reusable islands in context. Copy
`starter/essay.mdx.template` (or run `npm run create-essay`) to begin your own.

## The meta-lens radar

The `RadarChart` island plots one or more series on shared axes, normalized 0–1.
Pass a single series for an essay's own scores, or several to overlay a
comparison against the canonical meta-lens.

<RadarChart
  interactive
  axes={['Measure', 'Witness', 'Use', 'Cosmos', 'Power', 'Silence']}
  series={[
    { label: 'This essay', values: [0.8, 0.7, 0.6, 0.4, 0.55, 0.45] },
    {
      label: 'Corpus mean',
      values: [0.62, 0.58, 0.6, 0.55, 0.66, 0.55],
      color: 'var(--ink-muted)',
    },
  ]}
  caption="Hover a legend item to emphasize its series."
/>

## The adaptive timeline

`AdaptiveTimeline` compresses ancient centuries with the same square-root scale
the cross-essay atlas uses, so a millennium-spanning essay stays legible. Events
with an `href` become focusable links.

<AdaptiveTimeline
  ticks={[1500, 1700, 1850, 2024]}
  caption="Three landmark maps, plotted on a compressed scale."
  events={[
    { year: 1507, label: 'Waldseemüller world map' },
    { year: 1569, label: 'Mercator projection' },
    { year: 1973, label: 'GPS constellation begins', accent: 'var(--gained)' },
  ]}
/>

## The compare slider

`CompareSlider` reveals one layer over another via clip-path, driven by pointer,
touch, or keyboard (focus the handle and use the arrow keys). Drop images or SVG
into the `before` and `after` slots.

<CompareSlider
  start={50}
  beforeLabel="Projection A"
  afterLabel="Projection B"
  caption="Slide, or focus the handle and press ← / →."
>
  <svg slot="before" viewBox="0 0 800 360" xmlns="http://www.w3.org/2000/svg">
    <rect width="800" height="360" fill="#0e0c08" />
    <g stroke="#d4b87a" fill="none" opacity="0.6">
      {Array.from({ length: 9 }).map((_, i) => (
        <line x1={i * 100} y1="0" x2={i * 100} y2="360" />
      ))}
      {Array.from({ length: 5 }).map((_, i) => (
        <line x1="0" y1={i * 90} x2="800" y2={i * 90} />
      ))}
    </g>
  </svg>
  <svg slot="after" viewBox="0 0 800 360" xmlns="http://www.w3.org/2000/svg">
    <rect width="800" height="360" fill="#0e0c08" />
    <g stroke="#96cc84" fill="none" opacity="0.7">
      {Array.from({ length: 9 }).map((_, i) => (
        <path d={`M ${i * 100} 0 Q ${i * 100 + 40} 180 ${i * 100} 360`} />
      ))}
      {Array.from({ length: 5 }).map((_, i) => (
        <line x1="0" y1={i * 90} x2="800" y2={i * 90} />
      ))}
    </g>
  </svg>
</CompareSlider>

## The scrollytelling scaffold

`Scrollytelling` pairs a sticky `graphic` pane with a column of narrative steps.
Mark each step block `data-scrolly-step`; give the graphic ordered
`data-scrolly-figure` panels and it cross-fades to the one matching the active
step as the reader scrolls. It rides the shared IntersectionObserver scroll-spy
(`src/lib/scrollytelling.ts`) and is reduced-motion aware — under
`prefers-reduced-motion` the graphic un-sticks and every figure reads in flow.
Prefer a bespoke graphic? Listen for the `scrolly:step` event it dispatches.

<Scrollytelling label="How a projection unrolls the globe">
  <svg slot="graphic" data-scrolly-figure viewBox="0 0 320 320" xmlns="http://www.w3.org/2000/svg">
    <rect width="320" height="320" fill="#0e0c08" />
    <circle cx="160" cy="160" r="120" fill="none" stroke="#d4b87a" stroke-width="2" />
    <ellipse cx="160" cy="160" rx="120" ry="46" fill="none" stroke="#d4b87a" opacity="0.5" />
    <line x1="160" y1="40" x2="160" y2="280" stroke="#d4b87a" opacity="0.5" />
  </svg>
  <svg slot="graphic" data-scrolly-figure viewBox="0 0 320 320" xmlns="http://www.w3.org/2000/svg">
    <rect width="320" height="320" fill="#0e0c08" />
    <g fill="none" stroke="#d4b87a" stroke-width="2">
      <path d="M40 160 Q160 60 280 160" />
      <path d="M40 160 Q160 260 280 160" />
      <line x1="160" y1="60" x2="160" y2="260" opacity="0.5" />
    </g>
  </svg>
  <svg slot="graphic" data-scrolly-figure viewBox="0 0 320 320" xmlns="http://www.w3.org/2000/svg">
    <rect width="320" height="320" fill="#0e0c08" />
    <g fill="none" stroke="#96cc84" stroke-width="2">
      {Array.from({ length: 6 }).map((_, i) => (
        <line x1={40 + i * 48} y1="60" x2={40 + i * 48} y2="260" opacity="0.7" />
      ))}
      {Array.from({ length: 5 }).map((_, i) => (
        <line x1="40" y1={60 + i * 50} x2="280" y2={60 + i * 50} opacity="0.7" />
      ))}
    </g>
  </svg>

  <div data-scrolly-step>
    **The globe.** Start with the sphere — the only faithful model, and the one
    you can never lay flat on a table or a page.
  </div>
  <div data-scrolly-step>
    **The cut.** To flatten it you must tear it somewhere. Every projection is a
    decision about where that violence goes.
  </div>
  <div data-scrolly-step>
    **The grid.** Rolled onto a plane, the meridians snap parallel — legible,
    printable, and quietly lying about the poles.
  </div>
</Scrollytelling>

---

<Section id="on-abstraction" title="Why these components are theatre" rooms={['theatre', 'map']}>

This chapter belongs to a different room than the essay. It is wrapped in
`<Section>`, which emits the `#on-abstraction` anchor and declares its rooms —
mirroring the `sections` entry in this essay's frontmatter, the queryable source
of truth the room pages read. Every island above stages an argument about a map
rather than reproducing one, which is why this section is tagged **The Theatre**
(abstraction) as well as **The Map**.

</Section>

That's the whole toolkit. Everything above is server-rendered SVG/HTML; only the
compare slider and the radar's hover ship a few lines of vanilla JS. See
[the colophon](/colophon/) for the meta-lens crosswalk these components visualize.
